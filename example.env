# Google Vertex AI Configuration
VERTEXAI_PROJECT=gen-lang-client-0910640178
VERTEXAI_LOCATION=europe-west1

# Google Cloud Authentication
# Option 1: Use service account key file (recommended for development)
GOOGLE_APPLICATION_CREDENTIALS=./gemini-service-account.json

# Option 2: Use service account JSON directly (for production/containerized environments)
# GOOGLE_SERVICE_ACCOUNT_JSON={"type": "service_account", "project_id": "gen-lang-client-0910640178", ...}

# Optional: Model configuration (defaults shown)
GEMINI_MODEL=gemini-2.5-flash

# Phoenix configuration
PHX_HOST=localhost
SECRET_KEY_BASE=your_secret_key_base_here